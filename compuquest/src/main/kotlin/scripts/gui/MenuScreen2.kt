package scripts.gui

import godot.Button
import godot.Node
import godot.annotation.RegisterClass
import godot.annotation.RegisterFunction
import godot.core.variantArrayOf

@RegisterClass
class MenuScreen2 : Node() {
//  var conversations: List<MenuItem> = listOf()
//
//  @RegisterFunction
//  fun on_pressed(index: Int) {
//	val screen = newMenuScreen(conversations[index])
//	getParent()!!.addChild(screen)
//	queueFree()
//  }
//
//  @RegisterFunction
//  override fun _ready() {
//	val items = findNode("items")!!
//	conversations.forEachIndexed { index, conversation ->
//	  val button = Button()
//	  button.text = conversation.title
//	  items.addChild(button)
//	  button.connect("pressed", this, "on_pressed", variantArrayOf(index))
//	}
//  }
}
